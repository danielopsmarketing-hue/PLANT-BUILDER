{
  "name": "plant-builder-server",
  "version": "1.0.0",
  "private": true,
  "description": "Backend for Plant Builder: equipment catalog API, admin CRUD, and image uploads.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "multer": "^2.0.0"
  }
}
